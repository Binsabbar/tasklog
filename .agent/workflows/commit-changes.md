---
description: Commit changes using git-ci
---

1. Stage changes
   // turbo

   ```bash
   git add .
   ```

2. Commit changes
   Replace `[type]` and `[message]` with your commit details.

   ```bash
   git ci [type] "[message]"
   ```
